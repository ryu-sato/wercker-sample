box: alpine

# services:
#   # Docker daemon
#   - id: docker
#     name: dind
#     tag: stable-dind

build:
  steps:
    - script:
      name: stdout
      code: |
        echo "hello wercker"

test:
  steps:
    - script:
      name: stdout
      code: |
        echo "hello wercker"

    - script:
      name: ps
      code: |
        PSEF=`ps -ef`
        echo "ps -ef : ${PSEF}"

    - script:
      name: wait
      code: |
        echo "sleeping `date`"
        sleep 10
        echo "slept `date`"

    - internal/docker-run:
      image: docker
      name: get-dockers-info
      cmd: |
        docker version
        docker ps -a
        docker networks ls
        docker volume ls

