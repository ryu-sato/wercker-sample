box:
  id: alpine
  cmd: /bin/sh

# services:
#   # Docker daemon
#   - id: docker
#     name: dind
#     tag: stable-dind

build:
  steps:
    - internal/docker-run:
      image: docker
      cmd: |
        docker version
        docker ps -a
        docker networks ls
        docker volume ls
