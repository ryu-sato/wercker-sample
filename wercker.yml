box:
  id: alpine
  cmd: /bin/sh

# services:
#   # Docker daemon
#   - id: docker
#     name: dind
#     tag: stable-dind

build:
  steps:
    - script:
      name: stdout
      code: |
        echo "hello wercker"

test:
  steps:
    - script:
      name: stdout
      code: |
        echo "hello wercker"

    - internal/docker-run:
      image: docker
      name: get-dockers-info
      cmd: |
        docker version
        docker ps -a
        docker networks ls
        docker volume ls
